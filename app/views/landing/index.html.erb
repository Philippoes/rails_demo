<ul>
<% @articles.each do |article| %>
  <li>
    <%= article.title %>
    <%= article.content %>
    <p>Written by </p> <%= article.author %> <p> at <%= article.created_at.strftime("%Y-%m-%d") %> </p>
    <%= render partial: 'partials/comments' %>
  </li>
  <% end %>
</ul>

